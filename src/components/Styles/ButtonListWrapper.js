import styled from 'styled-components';

const ButtonListWrapper = styled.div`
  display: flex;
  flex-direction: row;
`;

ButtonListWrapper.displayName = 'ButtonListWrapper';

export default ButtonListWrapper;
